services:
  - type: web
    name: loan-backend
    runtime: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt && python manage.py migrate
    startCommand: gunicorn config.wsgi:application -c gunicorn.conf.py
    envVars:
      - key: DJANGO_SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "False"
      - key: USE_SQLITE
        value: "True"
      - key: ALLOWED_HOSTS
        value: "localhost,127.0.0.1,.onrender.com"
      - key: CORS_ALLOWED_ORIGINS
        value: "http://localhost:5173,https://easy-loans.onrender.com,https://loan-frontend.onrender.com"
      - key: CSRF_TRUSTED_ORIGINS
        value: "http://localhost:5173,https://easy-loans.onrender.com,https://loan-frontend.onrender.com"
      - key: JWT_ACCESS_MINUTES
        value: "30"
      - key: JWT_REFRESH_DAYS
        value: "1"
      - key: LOAN_MAX_AMOUNT
        value: "100000"
      - key: MAX_UPLOAD_SIZE_BYTES
        value: "5242880"

  - type: static
    name: loan-frontend
    rootDir: frontend
    buildCommand: npm ci && npm run build
    staticPublishPath: dist
    envVars:
      - key: VITE_API_BASE_URL
        value: "https://loan-backend.onrender.com/api"
      - key: VITE_LOAN_MAX_AMOUNT
        value: "100000"
